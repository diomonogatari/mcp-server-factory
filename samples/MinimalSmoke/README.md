# MinimalSmoke

Minimal runnable sample showing how to start an in-memory MCP server and call a
registered tool through `McpServerIntegrationFactory`.

## Run

```bash
dotnet run --project samples/MinimalSmoke/MinimalSmoke.csproj
```
