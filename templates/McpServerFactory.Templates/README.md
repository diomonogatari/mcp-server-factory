# McpServerFactory.Templates

Template package for `dotnet new` that bootstraps MCP integration tests using
`McpServerFactory`.

## Included templates

- `mcp-itest` — xUnit integration test scaffold for MCP server tools.

## Install

```bash
dotnet new install McpServerFactory.Templates
```

## Use

```bash
dotnet new mcp-itest -n MyServer.Tests
```
